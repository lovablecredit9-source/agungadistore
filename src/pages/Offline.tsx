import Index from "./Index";

// Offline mode renders the same full app UI
// Songs that are cached will be playable, uncached ones won't
const Offline = () => {
  return <Index />;
};

export default Offline;
